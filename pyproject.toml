[project]
name = "lightimer"
version = "1.0.0"
description = "A lightweight countdown timer for presentations"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.6.0",
    "ruff>=0.15.1",
    "pytest>=8.0",
    "pyinstaller>=6.18.0",
]
